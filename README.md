# Stellar Todos

A minimal Next.js to-do list with filtering, completion counts, and a polished neon-inspired theme.

## Getting started

Install dependencies and run the development server:

```bash
npm install
npm run dev
```

Then open http://localhost:3000 to interact with the app.
